# Load libraries.
library(readr)
library(lubridate)

# Load data directly.
detroit_df <- read_csv("")

# Load data (if you have already downloaded it).
detroit_df <- read_csv("data/detroit_cfs.csv")

# Explore data.
glimpse(detroit_df)